apiVersion: v1
kind: Service
metadata:
  name: recipe-rescue-web
  namespace: recipe-rescue
  labels:
    app.kubernetes.io/name: recipe-rescue-web
    app.kubernetes.io/component: frontend
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: recipe-rescue-web
    app.kubernetes.io/component: frontend
