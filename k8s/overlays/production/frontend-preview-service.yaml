apiVersion: v1
kind: Service
metadata:
  name: recipe-rescue-web-preview
  labels:
    app.kubernetes.io/name: recipe-rescue-web
    app.kubernetes.io/component: frontend
    app.kubernetes.io/traffic-role: preview
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: recipe-rescue-web
    app.kubernetes.io/component: frontend
