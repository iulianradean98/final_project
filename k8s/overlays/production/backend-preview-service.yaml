apiVersion: v1
kind: Service
metadata:
  name: recipe-rescue-api-preview
  labels:
    app.kubernetes.io/name: recipe-rescue-api
    app.kubernetes.io/component: backend
    app.kubernetes.io/traffic-role: preview
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: recipe-rescue-api
    app.kubernetes.io/component: backend
